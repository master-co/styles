import pageCategories from '~/data/page-categories.json'

---

## Variable
Variable references based on the Master Design System help you customize consistent web fonts, spacing, colors, and more.
<OverviewButtons>
    {pageCategories.find(category => category.name === 'Variable').pages}
</OverviewButtons>

---

## Configuration
<OverviewButtons>
    {pageCategories.find(category => category.name === 'Configuration').pages}
</OverviewButtons>

---

## Syntax
<OverviewButtons>
    {pageCategories.find(category => category.name === 'Syntax').pages}
</OverviewButtons>

---

## Package
<OverviewButtons>
    {pageCategories.find(category => category.name === 'Package').pages}
</OverviewButtons>
