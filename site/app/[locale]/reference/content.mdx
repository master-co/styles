import { getUnitCategories } from '~/site/metadata'

export const categories = getUnitCategories('reference')

## Design Token
<OverviewButtons>
    {categories.find(category => category.name === 'Design Token').definedMetadataList}
</OverviewButtons>

## Syntax
<OverviewButtons>
    {categories.find(category => category.name === 'Syntax').definedMetadataList}
</OverviewButtons>

## Package
<OverviewButtons>
    {categories.find(category => category.name === 'Package').definedMetadataList}
</OverviewButtons>
